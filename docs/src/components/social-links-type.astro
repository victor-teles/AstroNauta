---
import { socialLinks } from '../../../packages/astronauta/schemas/social';

const socials = [...socialLinks].sort((a, b) => a.localeCompare(b, 'en'));
---

<code>{`Partial<Record<${socials.map((social) => `'${social}'`).join(' | ')}, string>>`}</code>
