---
import Icon from '../user-components/Icon.astro';
import type { Props } from '../props';
import { useTranslations } from '../utils/translations';

const t = useTranslations(Astro.props.locale);
---

<p class="nta-flex">
	<Icon name={'warning'} size="1.5em" color="var(--nta-color-orange-high)" /><span
		>{t('i18n.untranslatedContent')}</span
	>
</p>

<style>
	p {
		border: 1px solid var(--nta-color-orange);
		padding: 0.75em 1em;
		background-color: var(--nta-color-orange-low);
		color: var(--nta-color-orange-high);
		width: max-content;
		max-width: 100%;
		align-items: center;
		gap: 0.75em;
		font-size: var(--nta-text-body-sm);
		line-height: var(--nta-line-height-headings);
	}
</style>
